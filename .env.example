# Copy to .env (gitignored) for docker-compose / local tools.
# Never commit .env with real secrets.

# Required for n8n container (example stack)
N8N_ENCRYPTION_KEY=change_this_to_a_long_random_string

# Optional: vault path for local scripts
# OBSIDIAN_VAULT_ROOT=/path/to/Vault
